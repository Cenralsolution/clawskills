# OpenClaw Skills Platform - Environment Configuration Template
# 
# ⚠️  IMPORTANT SECURITY REMINDERS:
# - Never commit this file to git (it's in .gitignore by default)
# - Never share API keys or credentials with anyone
# - Keep credentials in .env file only, never in source code
# - Rotate credentials regularly for security
# - Monitor API usage to detect unauthorized access
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env in the workspace root
# 2. Replace the placeholder values with your actual API keys
# 3. Keep .env in .gitignore (do NOT commit to git)
# 4. Ensure .env file has restrictive permissions (chmod 600)

# ============================================================================
# OPENAI / CHATGPT CONFIGURATION (Skill 1)
# ============================================================================

# Your OpenAI API key (REQUIRED for ChatGPT Prompt Generator)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# OpenAI model to use (optional, defaults to gpt-4)
# Options: gpt-4, gpt-4-turbo-preview, gpt-3.5-turbo
OPENAI_MODEL=gpt-4

# OpenAI API request timeout in seconds (optional, defaults to 30)
OPENAI_TIMEOUT=30

# ============================================================================
# MUSIC GENERATION PROVIDERS (Skills 2-5)
# ============================================================================

# AIVA Music Generator (optional, required if using AIVA provider)
# Get from: https://www.aiva.ai/api
AIVA_API_KEY=your-aiva-api-key-here

# Replicate Music Generator (optional, required if using Replicate provider)
# Get from: https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=r8_your-replicate-api-token-here

# Mubert Music Generator (optional, required if using Mubert provider)
# Get from: https://mubert.com/api
MUBERT_API_KEY=your-mubert-api-key-here

# Soundraw Music Generator (optional, required if using Soundraw provider)
# Get from: https://www.soundraw.io/api
SOUNDRAW_API_KEY=your-soundraw-api-key-here

# ============================================================================
# MUSIC OUTPUT CONFIGURATION
# ============================================================================

# Directory where generated music files are saved (optional, defaults to ./generated_music)
MUSIC_OUTPUT_DIR=./generated_music

# ============================================================================
# GITHUB INSTALLER CONFIGURATION (Skill 6)
# ============================================================================

# Your GitHub Personal Access Token (optional, required for GitHub Installer skill)
# Create at: https://github.com/settings/tokens
# Required scopes: repo (full control), read:org
GITHUB_TOKEN=ghp_your-github-personal-access-token-here

# GitHub API base URL (optional, defaults to https://api.github.com)
GITHUB_API_BASE_URL=https://api.github.com

# GitHub API request timeout in seconds (optional, defaults to 30)
GITHUB_TIMEOUT=30

# Enable automatic backups before replacing skills (optional, defaults to true)
BACKUP_ENABLED=true

# ============================================================================
# GITHUB OAUTH CONFIGURATION (Optional - for GUI-based authentication)
# ============================================================================

# Google OAuth Client ID (optional, required for OAuth support)
# Create OAuth app at: https://console.cloud.google.com/apis/credentials
GOOGLE_OAUTH_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com

# Google OAuth Client Secret (optional, KEEP SECRET!)
# Create OAuth app at: https://console.cloud.google.com/apis/credentials
GOOGLE_OAUTH_CLIENT_SECRET=your-google-oauth-client-secret-here

# Google OAuth Redirect URI (optional, defaults to http://localhost:8080/callback)
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:8080/callback

# ============================================================================
# SUB-AGENT MONITOR & NOTIFIER CONFIGURATION (Skill 7)
# ============================================================================

# Monitoring data storage directory (optional, defaults to ./monitor_data)
MONITOR_STORAGE_DIR=./monitor_data

# -------- Email Notifications --------
# Email configuration for notifications (optional, required if using email channel)
NOTIFICATION_EMAIL_CONFIG=alerts@example.com

# Email server username (optional, required for SMTP authentication)
NOTIFICATION_EMAIL_USER=your-email@gmail.com

# Email server password (optional, KEEP SECRET!)
# For Gmail: Use app-specific password, not your main password
# Get at: https://support.google.com/accounts/answer/185833
NOTIFICATION_EMAIL_PASSWORD=your-app-specific-password-here

# Email recipients (comma-separated, optional)
NOTIFICATION_EMAIL_TO=admin@example.com,ops@example.com

# -------- Webhook Notifications --------
# Webhook URL for status change notifications (optional, required if using webhook channel)
# This URL receives JSON POST requests with agent status updates
# Example: https://your-api.com/webhooks/agent-alerts
# ⚠️  This is a sensitive credential - never log or expose it
NOTIFICATION_WEBHOOK_URL=https://your-api.com/webhooks/agent-alerts

# Webhook request timeout in seconds (optional, defaults to 10)
NOTIFICATION_WEBHOOK_TIMEOUT=10

# -------- Slack Notifications --------
# Slack Incoming Webhook URL for alerts (optional, required if using Slack channel)
# Create at: https://api.slack.com/messaging/webhooks
# Format: https://hooks.slack.com/services/YOUR/WEBHOOK/URL
# ⚠️  This is a sensitive credential - never log or expose it
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Slack message username (optional, defaults to 'OpenClaw Monitor')
SLACK_WEBHOOK_USERNAME=OpenClaw Monitor

# -------- SMS Notifications --------
# SMS provider for notifications (optional, defaults to twilio)
# Supported: twilio, nexmo, vonage
SMS_PROVIDER=twilio

# SMS configuration for notifications (optional, required if using SMS channel)
NOTIFICATION_SMS_CONFIG=your-sms-config-here

# Twilio Account SID (optional, required if SMS_PROVIDER=twilio)
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxx

# Twilio Auth Token (optional, KEEP SECRET! required if SMS_PROVIDER=twilio)
TWILIO_AUTH_TOKEN=your-twilio-auth-token-here

# Twilio Phone Number for sending SMS (optional, required if using Twilio SMS)
TWILIO_PHONE_NUMBER=+1234567890

# SMS alert recipient numbers (comma-separated, optional)
SMS_ALERT_NUMBERS=+9876543210,+9876543211

# ============================================================================
# GENERAL CONFIGURATION
# ============================================================================

# Logging level (optional, defaults to INFO)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log file path (optional, defaults to ./openclaw.log)
LOG_FILE=./openclaw.log

# Enable detailed logging (optional, defaults to false)
# ⚠️  WARNING: May expose sensitive data in debug logs, only enable in development!
DETAILED_LOGGING=false

# Enable test mode with mock API responses (optional, defaults to false)
TEST_MODE=false

# ============================================================================
# SECURITY CONFIGURATION
# ============================================================================

# Enable HTTPS certificate verification (optional, defaults to true)
VERIFY_HTTPS=true

# Maximum request size in MB (optional, defaults to 100)
MAX_REQUEST_SIZE_MB=100

# Request rate limit (requests per minute, optional, defaults to 600)
RATE_LIMIT_PER_MINUTE=600

# ============================================================================
# SECURITY BEST PRACTICES
# ============================================================================
#
# ✅ DO:
# - Store all credentials in .env file only
# - Use unique, strong API keys and tokens
# - Keep .env file in .gitignore
# - Rotate credentials regularly (at least quarterly)
# - Use restrictive file permissions (chmod 600 .env)
# - Monitor API usage for unauthorized access
# - Use HTTPS for all API communications
# - Treat webhook URLs as secrets (they're authentication tokens)
#
# ❌ DON'T:
# - Hard-code credentials in source code
# - Commit .env or credentials to git
# - Share API keys via email or chat
# - Use test/staging credentials in production
# - Log credentials or print them to console
# - Use HTTP for credential transmission
# - Reuse API keys across multiple services
# - Leave old/unused credentials active
#
# If API keys are accidentally exposed:
# 1. Go to the provider dashboard immediately
# 2. Regenerate/revoke all exposed keys
# 3. Update this .env file with new keys
# 4. Redeploy all applications
# 5. Monitor for unauthorized usage
#
# ============================================================================
# PROVIDER DASHBOARDS
# ============================================================================
#
# OpenAI API Keys:
#   https://platform.openai.com/api-keys
#   Billing: https://platform.openai.com/account/billing/overview
#
# AIVA API Settings:
#   https://www.aiva.ai/settings
#
# Replicate API Tokens:
#   https://replicate.com/account/api-tokens
#
# Mubert API Settings:
#   https://mubert.com/settings
#
# Soundraw Account Settings:
#   https://www.soundraw.io/account
#
# GitHub Personal Tokens:
#   https://github.com/settings/tokens
#
# Google OAuth Apps:
#   https://console.cloud.google.com/apis/credentials
#
# Slack Webhooks:
#   https://api.slack.com/messaging/webhooks
#
# Twilio Account:
#   https://www.twilio.com/console
#
# ============================================================================
